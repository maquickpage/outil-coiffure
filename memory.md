# Outil-Coiffure Memory

## Active Decisions
- **Résiliation pendant le premier mois (2026-07-27)** : les trois formules (`FLEX`, `ONE_YEAR`, `TWO_YEAR`) peuvent être résiliées sur demande reçue dans les 30 jours calendaires suivant le premier paiement. La première mensualité n'est pas remboursée ; le service reste actif jusqu'à la fin du premier mois payé puis le site est mis hors ligne ; aucun prélèvement supplémentaire n'est effectué. Pour `ONE_YEAR` et `TWO_YEAR`, une demande valable dans ce délai lève entièrement l'engagement et toute indemnité sur les mensualités restantes. Le domaine n'est pas transféré automatiquement (transfert selon CGV). Après 30 jours, les règles normales de chaque formule s'appliquent. CGV version `1.1` ; le checkout doit afficher directement non-remboursement + levée d'engagement, le détail pouvant être en FAQ.
- **Per-salon template (2026-07-10)** : colonne `salons.template` (`classic` défaut | `contrast` | `drama`). `classic` = site historique `public/site/index.html` (zéro régression prod) ; `contrast`/`drama` = design kit `templates/<id>/` rendu par `src/ssr.js` (réécriture des assets vers `/_templates/<id>/` + injection de la chrome de vente). Choix exposé dans l'éditeur coiffeur (`/admin/{slug}` → onglet « Design du site », PUT `/api/edit/:slug` champ `template`).
- **Services par CATÉGORIE (2026-07-11, remplace l'option B du 07-10)** : champ `category` (`femme`|`homme`|`autres`) sur chaque service — défauts (`src/defaults.js`), fixtures, et select par service dans l'éditeur. Sur MOBILE les 3 templates affichent 1 slide de carrousel par catégorie (liste dedans) ; desktop inchangé (classic garde son carrousel de cartes >4 items). Champ absent → `autres`.
- **Contrast : largeur commune (2026-07-11)** : toutes les sections s'alignent sur `--content-max: 1600px` (= galerie) ; titre contact centré comme les autres bandes ; carte dans la même gouttière (plus de pleine largeur).
- **design.monochromePhotos (2026-07-11)** : toggle « Photos en noir & blanc » dans l'éditeur (onglet Design du site), défaut `true`. `false` → Contrast/Drama retirent leurs filtres N&B (le hero garde un léger assombrissement pour la lisibilité). Sans effet sur Classic. Section `overrides.design`.
- **intro.image (2026-07-11)** : photo d'illustration de la section intro (utilisée par Contrast), éditable dans l'admin (onglet Texte d'intro, upload kind='intro' 4:5). Vide → le template prend automatiquement une photo de la galerie ≠ hero (fallback hero) ; l'éditeur affiche le même choix avec un badge « Choisie automatiquement ».
- Templates : `<title>` + `<meta name="description">` doivent rester ADJACENTS dans chaque `templates/*/index.html` (regex SSR).
- **Suivi landing maquickpage.fr (2026-07-12)** : funnel ANONYME de la home marketing, réutilise la table `preview_events` avec `slug=NULL` et le namespace `landing_*` (view/scroll/cta/check_open/check_submit/check_found/check_notfound). `landing_view` = middleware serveur sur `GET /` en routingMode 'landing' (`src/routes/tracking.js`) ; les autres = beacons `window.mqsTrack` (track.js chargé par home.html) câblés dans `home.js` ; found/notfound loggés dans `src/routes/landing.js`. Agrégat : `GET /admin/api/landing-stats.json` (admin.js, filtre bots par UA). Vue : nouvel onglet **Suivi landing** = `public/admin/stats-landing.html` (KPIs + entonnoir + répartition CTA + tendance 14 j, FR/EN/中). Ajouter toute page admin statique à `RESERVED_ADMIN_PATHS` (server.js) sinon `/admin/:slug` la traite comme un slug salon (éditeur) hors host admin.
- **Landing funnel TRAÇABLE, sans cookie (2026-07-12)** : clé visiteur = `ip | user_agent` (choix cookieless = pas de bannière CNIL, cohérent avec « Appareils & connexions » du Suivi salon). 100% read-time : AUCUN nouveau tracking — tous les events `landing_*` et `landing_leads` portent déjà ip+ua. `GET /admin/api/landing-stats.json` reconstruit le parcours par visiteur et rattache l'email (landing_leads) au parcours anonyme par la même clé → renvoie `visitorFunnel` (entonnoir par visiteur unique), `leads` (email + parcours complet), `engaged` (a cliqué un CTA sans envoyer). Pré-requis du join : `clientIp()` exporté de tracking.js et utilisé AUSSI dans landing.js (même dérivation IP des 2 côtés). ⚠️ `landing_leads` a `created_at` (PAS `ts`) — aliaser `l.created_at AS ts` dans le SELECT. La page affiche entonnoir visiteur-unique + table Leads (lignes dépliables → timeline du parcours + ip/ua) + table Engagés. Pas d'attribution source (UTM/referrer) pour l'instant — extension future via champ `src`.
- **Principe « entonnoir honnête » (2026-07-13, PR #4 déployée)** : l'entonnoir ne compte QUE les étapes observées (events) — jamais d'inférence (un submit ne rétro-remplit pas scroll/CTA). Leads sans events rapprochés = « orphelins » (pré-tracking, ou ancienne IP stockée = proxy Cloudflare 104.16.0.0/13 car l'ancien code lisait `req.ip`) : listés « Aucun parcours enregistré », EXCLUS de l'entonnoir visiteurs. Parcours découpés en visites (gap > 30 min). Limites cookieless ACCEPTÉES sciemment : clé ip|ua coarse (UA Chrome figé → collisions possibles), `/api/track` non rate-limité (spoofable), lecture 500k rows par affichage — à revisiter seulement si le trafic le justifie.
- **Suivi maquettes — agrégat serveur + entonnoir prospect honnête (2026-07-13, PR #5 déployée)** : `/admin/stats.html` ne parse plus le CSV 200k lignes côté client. Nouvel endpoint `GET /admin/api/suivi.json` agrège serveur (N salons), **sans edit_token** (lien « Voir » → redirection auth `/api/suivi/preview/:slug`; export CSV sans colonne token). Entonnoir par **visiteur (ip|ua)**, bots + **IP internes** exclus. IP internes = table `suivi_excluded_ips`, togglée par `POST /api/suivi/exclude-ip` + bouton « Exclure mon trafic » (montre ton IP, choix de Michele = exclusion manuelle par IP, pas auto). Heat/status/tags d'un salon calculés sur les seuls appareils prospect → ton QA ne gonfle plus l'entonnoir. « réel » = slug ∈ salons (pas « nom joint non vide »). « visites » = vraies sessions (gap 30 min). Principe partagé avec le fix landing : **ne compter que l'observé, jamais l'inféré**, KPI & entonnoir même périmètre.
- **Vrais visiteurs + provenance (2026-07-18, PR #8 déployée)** : le KPI « visiteurs » landing était gonflé par les hits serveur sans JS (bots/scanners passant le filtre UA). Nouveau beacon `landing_ready` (home.js, envoyé au chargement, meta.ref = hostname du referrer) = « vrai navigateur ». Dashboard `stats-landing.html` refondu : KPI headline = **Vrais visiteurs (JS confirmé)** vs Visiteurs bruts (serveur), entonnoir basé sur les vrais visiteurs (fallback bruts si données pré-beacon), section « D'où viennent les vrais visiteurs » (`?src=` prioritaire, sinon referrer, sinon direct), source affichée sur chaque lead/engagé, tendance 14 j avec barre « vrais visiteurs ». ⚠️ Le SELECT de `/admin/api/landing-stats.json` doit inclure la colonne `src` (bug corrigé le 18/07 : `r.src` était undefined sans elle).
- ⚠️ Piège de portage : `main` évolue pendant que `feat/templates-design-kit` traîne — un `cp` wholesale de `src/routes/admin.js` du feature branch vers main REVERTERAIT `recaptureAsync` (commit 855791e). Toujours porter par edit chirurgical dans un worktree off origin/main, et vérifier `git diff` hunk par hunk avant commit.

- **Séquenceur cold-email dans l'admin (2026-07-31)** : nouvel onglet « 📨 Séquenceur » (`public/admin/sequencer.html` + `src/routes/sequencer.js`, derrière requireAuth). Le portail est un pur control plane : aucun lead stocké ici — il proxifie en JSON (token) vers des nœuds Apps Script (1 nœud = 1 boîte Gmail = le MVP de bibiproject/03-email-outreach/infra/mvp/), registre en table `sequencer_nodes`. Import CSV round-robin réparti par le portail (pré-validation totale, colonne mailbox forcée par nœud), steps broadcastés à tous les nœuds, suppression partagée poussée à tous. Les nœuds doivent être déployés en web app accès « Anyone » + `initApiToken()`.

## Tech Stack
- Express + better-sqlite3, SSR regex-based (src/ssr.js), templates statiques hydratés par `window.__SALON_VIEW__`.

## Known Issues
- `npm test` échoue (`node --test tests/` ne globbe pas sur Node 22.12) ; lancer `node --test tests/smoke.test.mjs`.

## Assumptions to Test
- La bannière CTA (`banner.js`) s'affiche correctement sur contrast/drama en conditions réelles de démo (gating non reproduit en local).
